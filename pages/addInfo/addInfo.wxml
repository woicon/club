<form bindsubmit='saveForm'>
    <input class='hidden' name="type" value='0'></input>
    <input class='hidden' name="status" value='1'></input>
    <input class='hidden' name="fieldType" value="{{type}}"></input>
    <view class='inputs ' wx:if="{{type== 0}}">
        <input placeholder='单行文本报名填写项' name="name"></input>
    </view>
    <view class='inputs' wx:elif="{{type== 1}}">
        <input placeholder='单行文本报名填写项' class='bline' name="name"></input>
        <view class='input-info'>
            <view class='ico ico-info'></view>需要报名者填写文字较多的信息，如个人简介、项目介绍、团队介绍，请使用本功能</view>
    </view>
    <block wx:elif="{{type== 2 || type== 3}}">
        <view class='input-block'>
            <input placeholder='{{type== 2 ? "输入单选问题" :"输入多选问题" }}' name="name" class='bline'></input>
            <view class='input-info'>
                <view class='ico ico-info'></view>至少填写两项</view>
            <view class='input-list'>
                <view class='bline' wx:for="{{types}}" wx:key="undlines">
                    <view class='ico ico-blocked' wx:if="{{index!=0 && index!= 1}}" bindtap='deleteCol' hover-class='touch' data-index="{{index}}"></view>
                    <input placeholder='选项{{index+1}}' data-index="{{index}}" bindinput='bindValue'></input>
                </view>
            </view>
            <view class='add' bindtap='addCol'>
                <view class='ico ico-plus'></view>
            </view>
        </view>
    </block>
    <view class='form-bar'>
        <button class='button' hover-class='button-touch' form-type='submit'>保存</button>
    </view>
</form>