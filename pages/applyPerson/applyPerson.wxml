<form>
    <view class='person'>
        <view class='forms'>
            <view class='f-head bline'>
                <view class='f-title'>第一位参与者</view>
                <view class='f-check'><view class='ico ico-checkbox-checked'></view> 设置为默认联系人</view>
            </view>
            <view>
                <label>姓名</label>
                <input placeholder='输入姓名' name="name"></input>
            </view>
            <view>
                <label>手机号</label>
                <input placeholder='输入手机号' type='number' maxlength='11' name="phone"></input>
            </view>
            <view>
                <label>您觉得我们的活动体验如何？</label>
                <radio-group  class="group" bindchange="radioChange">
                    <label class="radio" wx:for="{{items}}">
                        <radio value="{{item.name}}" checked="{{item.checked}}" />{{item.value}}
                    </label>
                </radio-group>
            </view>
            <view>
                <label>您对我们这次活动有什么建议？</label>
                <checkbox-group bindchange="checkboxChange" class="group">
                    <label class="checkbox" wx:for="{{items}}">
                        <checkbox value="{{item.name}}" checked="{{item.checked}}" />{{item.value}}
                    </label>
                </checkbox-group>
            </view>
            <label>您对我们这次活动有什么更好的建议？</label>
            <textarea>多行文本</textarea>
        </view>
    </view>
    <view class='button-bar'>
        <button form-type='submit' class='button' hover-class='button-touch'>提交报名</button>
    </view>
</form>