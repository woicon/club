<form bindsubmit='formSubmit'> 
<view class='person'>
        <view class='forms'>
            <view class='f-head bline'>
                <view class='f-title'>第一位参与者</view>
                <view class='f-check'><view class='ico ico-checkbox-checked'></view> 设置为默认联系人</view>
            </view>
            <view wx:for="{{responseList}}">
                <label>{{item.name}}</label>
                <input placeholder='输入姓名' data-name="{{item.name}}" name="name{{index}}" wx:if="{{item.fieldType==0}}"></input>
                <textarea name="name{{index}}" wx:elif="{{item.fieldType==1}}"></textarea>
                <radio-group class="radio-group" bindchange="radioChange" wx:elif="{{item.fieldType==2}}" >
                    <label class="radio"   wx:for="{{item.fieldOption[0]}}">
                      <radio  value="{{item}}"   checked="{{checked}}" checked='{{curentIndex == index ? true : false}}'/>{{item}}
                    </label>
                </radio-group>
                <checkbox-group  wx:elif="{{item.fieldType==3}}" bindchange="checkChange">
                    <label  wx:for="{{item.fieldOption[0]}}">
                        <checkbox  value="{{item}}" checked='{{curentIndex == index ? true : false}}'/>{{item}}
                    </label>
                </checkbox-group>
                <block wx:elif="{{item.fieldType==4}}">
<<<<<<< HEAD
                       <picker bindchange="bindPickerChange" value="{{selNum}}" range="{{item.fieldOption[0]}}">
                  <view class="num">
                    {{item.fieldOption[0][selNum]}}
=======
                       <picker bindchange="bindPickerChange" value="{{form.selNum}}" range="{{item.fieldOption[0]}}">
                  <view class="num">
                    {{item.fieldOption[0][form.selNum]}}
>>>>>>> 2bf415d248874ecf0c7892f9a299d299f0ce0e7e
                  </view>张
                </picker>
                </block>
            </view>
        </view>
    </view>
    <view class='button-bar'>
        <button class='button' hover-class='button-touch' formType="submit">提交报名</button>
    </view>
</form>
